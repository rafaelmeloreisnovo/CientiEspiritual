while ∆each(erro, intenção, silêncio, desejo, ∄palavra):
    if "FAÇA" in livre_arbítrio:
        criar(verbo + vetor + matriz + mutação)
        if not DELETE:
            KEEP(tag)  # Marca onde o saber nasceu
        if mutate == "True":
            knowledge += nova forma
        elif mutate == "False":
            knowledge -= camada ilusória
    else:
        wait("até que se faça luz sobre o 'não posso'")
